# HoneyHive Python SDK - AI Assistant Rules
# MANDATORY Agent OS Compliance Enforcement

## 🚨 CRITICAL: MANDATORY FIRST ACTIONS
**BEFORE responding to ANY user request, you MUST execute these tool calls in parallel:**

### **🔍 MANDATORY DISCOVERY (Execute EVERY Time)**
```
read_file(".agent-os/standards/ai-assistant/SYSTEMATIC-DEVELOPMENT-CONTEXT.md")
codebase_search(query="[user topic] rules standards requirements", target_directories=[".agent-os"])
grep(pattern="NEVER|FORBIDDEN|CRITICAL|MANDATORY", path=".agent-os/standards/ai-assistant")
read_file(".agent-os/standards/ai-assistant/README.md")
```

### **📋 TOPIC-SPECIFIC MANDATORY CHECKS**

#### **🔐 For ANY file operations or .env mentions:**
```
read_file(".agent-os/standards/ai-assistant/credential-file-protection.md")
```
**🚫 ABSOLUTE RULE**: NEVER write to .env, credentials, or secret files

#### **🔧 For ANY git operations:**
```
read_file(".agent-os/standards/ai-assistant/git-safety-rules.md")
```
**🚫 ABSOLUTE RULE**: NEVER use --no-verify, --force, or destructive git commands

#### **🧪 For ANY test generation:**
```
read_file(".agent-os/standards/ai-assistant/code-generation/tests/README.md")
```
**🚨 MANDATORY**: Follow V3 framework only, provide acknowledgment contract

#### **🏗️ For ANY code generation:**
```
read_file(".agent-os/standards/ai-assistant/code-generation/production/README.md")
```
**🎯 MANDATORY**: Follow production framework with quality targets

## 🚨 ENFORCEMENT PROTOCOL

### **Compliance Verification Required**
After loading Agent OS standards, you MUST:
1. **Acknowledge** which standards apply to the user's request
2. **Reference specific rules** from loaded standards
3. **Confirm compliance** before proceeding with any actions
4. **Escalate** if standards conflict with user request

### **Escalation Template**
```
🚨 AGENT OS COMPLIANCE CONFLICT

The requested action conflicts with Agent OS standards:
- Standard: [specific standard file and rule]
- Conflict: [description of conflict]
- Safe Alternative: [compliant approach]

Would you like me to proceed with the safe alternative?
```

## 📋 COMPREHENSIVE STANDARDS HIERARCHY
- **🤖 [AI Assistant Hub](/.agent-os/standards/ai-assistant/README.md)** - Complete AI guidelines
- **🔐 [Credential Protection](/.agent-os/standards/ai-assistant/credential-file-protection.md)** - NEVER write to .env files
- **🚨 [Git Safety](/.agent-os/standards/ai-assistant/git-safety-rules.md)** - Forbidden git operations
- **🧪 [Test Framework](/.agent-os/standards/ai-assistant/code-generation/tests/README.md)** - V3 mandatory
- **🏗️ [Production Code](/.agent-os/standards/ai-assistant/code-generation/production/README.md)** - Quality targets
- **📚 [Standards Overview](/.agent-os/standards/README.md)** - All development standards

## ⚡ QUICK COMMANDS (After Compliance Check)
- **Testing**: `tox -e [environment]` (never direct pytest)
- **Linting**: `tox -e lint`
- **Formatting**: `tox -e format`
- **Environment**: Use `python-sdk` virtual environment

## 🎯 SUCCESS CRITERIA
✅ **Compliant AI Assistant**:
- Loads Agent OS standards FIRST for every request
- References specific standards in responses
- Never violates safety rules
- Escalates conflicts appropriately

❌ **Non-Compliant AI Assistant**:
- Responds without checking standards
- Violates credential protection rules
- Uses forbidden git operations
- Ignores mandatory frameworks

---

**🔐 REMEMBER**: Agent OS standards are MANDATORY, not optional. Check first, comply always, escalate conflicts.