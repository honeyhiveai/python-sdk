# OpenAI DSL Translation - Implementation Roadmap
# =================================================

📅 TIMELINE: 2-3 Days
📊 CURRENT COVERAGE: 30%
🎯 TARGET COVERAGE: 100%

## PHASE 1: Navigation Rules (Day 1, 4 hours)
┌─────────────────────────────────────────────────────────────┐
│ FILE: config/dsl/providers/openai/navigation_rules.yaml    │
├─────────────────────────────────────────────────────────────┤
│ ADD:                                                         │
│  ✓ traceloop_tool_calls_flattened (CRITICAL)               │
│  ✓ openinference_tool_calls                                │
│  ✓ openlit_tool_calls_flattened                           │
│  ✓ traceloop_refusal                                       │
│  ✓ traceloop_audio                                         │
│  ✓ traceloop_system_fingerprint                           │
└─────────────────────────────────────────────────────────────┘

## PHASE 2: Transform Config (Day 1, 3 hours)
┌─────────────────────────────────────────────────────────────┐
│ FILE: config/dsl/providers/openai/transforms.yaml          │
├─────────────────────────────────────────────────────────────┤
│ ADD:                                                         │
│  ✓ extract_tool_calls (CRITICAL)                           │
│    - Uses: reconstruct_array_from_flattened                │
│    - Preserves: arguments as JSON string                   │
│  ✓ extract_refusal                                         │
│  ✓ extract_audio                                           │
│  ✓ extract_system_fingerprint                             │
└─────────────────────────────────────────────────────────────┘

## PHASE 3: Field Mappings (Day 1, 2 hours)
┌─────────────────────────────────────────────────────────────┐
│ FILE: config/dsl/providers/openai/field_mappings.yaml      │
├─────────────────────────────────────────────────────────────┤
│ FIX:                                                         │
│  ✓ tool_calls mapping (connect to extract_tool_calls)      │
│ ADD:                                                         │
│  ✓ outputs.refusal                                         │
│  ✓ outputs.audio                                           │
│  ✓ outputs.annotations                                     │
│  ✓ metadata.system_fingerprint                            │
│  ✓ metadata.response_model                                │
└─────────────────────────────────────────────────────────────┘

## PHASE 4: Transform Registry (Day 2, 2 hours)
┌─────────────────────────────────────────────────────────────┐
│ FILE: transform_registry.py                                 │
├─────────────────────────────────────────────────────────────┤
│ VERIFY EXISTS:                                              │
│  ✓ reconstruct_array_from_flattened (should exist)        │
│  ✓ extract_nested_array (may need to add)                 │
│  ✓ extract_nested_object (may need to add)                │
│                                                             │
│ CRITICAL: Add preserve_json_strings parameter!             │
└─────────────────────────────────────────────────────────────┘

## PHASE 5: Recompile Bundle (Day 2, 1 hour)
┌─────────────────────────────────────────────────────────────┐
│ ACTIONS:                                                     │
├─────────────────────────────────────────────────────────────┤
│  1. Run DSL compiler (or auto-recompile)                   │
│  2. Generate compiled_providers.pkl                         │
│  3. Verify bundle contents                                  │
│     - Check tool_calls extraction function exists          │
│     - Check all new transforms registered                  │
└─────────────────────────────────────────────────────────────┘

## PHASE 6: Testing (Day 3, 4 hours)
┌─────────────────────────────────────────────────────────────┐
│ FILE: tests/integration/test_openai_dsl_translation.py     │
├─────────────────────────────────────────────────────────────┤
│ TEST CASES:                                                 │
│  ✓ Tool calls reconstruction from flattened                │
│  ✓ Arguments preserved as JSON STRING                      │
│  ✓ Refusal messages extracted                              │
│  ✓ Audio responses extracted                               │
│  ✓ All 3 instrumentors (OpenInference, Traceloop, OpenLit)│
│  ✓ End-to-end with HoneyHive SDK                          │
└─────────────────────────────────────────────────────────────┘

## CRITICAL DATA FLOW
═══════════════════════════════════════════════════════════════

BEFORE (BROKEN):
┌────────────────────────────────────────────────────────────┐
│ Flattened Attributes                                        │
│ gen_ai.completion.0.message.tool_calls.0.id = "call_abc"  │
│ gen_ai.completion.0.message.tool_calls.0.function.name    │
│    = "get_weather"                                         │
└────────────────────────────────────────────────────────────┘
                    ↓ DSL (incomplete)
┌────────────────────────────────────────────────────────────┐
│ honeyhive_outputs.??? = ???                                │
│ ❌ Tool calls LOST!                                        │
└────────────────────────────────────────────────────────────┘

AFTER (FIXED):
┌────────────────────────────────────────────────────────────┐
│ Flattened Attributes                                        │
│ gen_ai.completion.0.message.tool_calls.0.id = "call_abc"  │
│ gen_ai.completion.0.message.tool_calls.0.function.name    │
│    = "get_weather"                                         │
│ gen_ai.completion.0.message.tool_calls.0.function.        │
│    arguments = '{"location": "SF"}'                       │
└────────────────────────────────────────────────────────────┘
                    ↓ DSL (complete)
┌────────────────────────────────────────────────────────────┐
│ honeyhive_outputs.tool_calls = [                           │
│   {                                                         │
│     "id": "call_abc",                                      │
│     "function": {                                          │
│       "name": "get_weather",                               │
│       "arguments": '{"location": "SF"}'  # JSON STRING!   │
│     }                                                       │
│   }                                                         │
│ ]                                                          │
│ ✅ Tool calls PRESERVED!                                   │
└────────────────────────────────────────────────────────────┘

## SUCCESS CRITERIA CHECKLIST
═══════════════════════════════════════════════════════════════

### P0 - MUST HAVE
□ Tool calls extracted from ALL instrumentors
□ Arguments preserved as JSON strings (NOT objects)
□ Array reconstruction works correctly
□ Bundle compiles without errors
□ Integration tests pass 100%

### P1 - SHOULD HAVE
□ Refusal messages extracted
□ Audio responses extracted
□ System fingerprint captured
□ Response model tracked

### P2 - NICE TO HAVE
□ Annotations extracted
□ Logprobs extracted
□ Legacy function_call support

## FILES TO MODIFY
═══════════════════════════════════════════════════════════════

Day 1:
  ✎ config/dsl/providers/openai/navigation_rules.yaml
  ✎ config/dsl/providers/openai/transforms.yaml
  ✎ config/dsl/providers/openai/field_mappings.yaml

Day 2:
  ✎ src/honeyhive/tracer/processing/semantic_conventions/transform_registry.py
  ⚙ Recompile: compiled_providers.pkl

Day 3:
  ✎ tests/integration/test_openai_dsl_translation.py
  ✓ Run tests & verify

## QUICK COMMANDS
═══════════════════════════════════════════════════════════════

# Check current state
grep -r "tool_calls" config/dsl/providers/openai/
grep "reconstruct_array_from_flattened" transform_registry.py

# After updates, recompile
rm compiled_providers.pkl
python -c "from honeyhive.tracer.processing.semantic_conventions.universal_processor import UniversalSemanticConventionProcessor"

# Run tests
pytest tests/integration/test_openai_dsl_translation.py -v

═══════════════════════════════════════════════════════════════
📚 Full Details: .agent-os/standards/architecture/OPENAI_DSL_IMPLEMENTATION_PLAN.md
═══════════════════════════════════════════════════════════════
